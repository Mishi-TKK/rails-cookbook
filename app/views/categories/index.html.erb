<h1>Cookbook</h1>
<h2>Categories</h2>
<ul>
  <% @categories.each do |category| %>
    <li>
      <%= link_to category.name, category_path(category) %> <!-- Link to the category's show page -->
    </li>
  <% end %>
</ul>
<%= link_to "add new category", new_category_path %>
